# Legacy provider fixtures

Minimal manifests of the legacy Work Suite and Ponytail providers, which ship from `ourostack/ouroboros-skills` rather than this repository. Desk still validates an explicitly declared legacy configuration, so these tests keep that path covered without shipping the legacy plugins here.
